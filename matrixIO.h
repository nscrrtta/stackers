// Matrix I/O
#define COLOUR_BUTTON     2 // 1 if pushed, 0 if not pushed
#define STACKERS_BUTTON  3 // 1 if pushed, 0 if not pushed
#define LEDS_PIN         4

// Matrix dimensions and number of LED's
#define NUM_COLUMNS  9
#define NUM_ROWS     16
#define NUM_LEDS     NUM_COLUMNS*NUM_ROWS
